import React from 'react'

const Searchbox = () => (
    <div className="searchBox">
        <form>
            <input type="search" placeholder="Rechercher dans la documentation"/>
            <button type="submit"></button>
        </form>
    </div>
);

export default Searchbox;